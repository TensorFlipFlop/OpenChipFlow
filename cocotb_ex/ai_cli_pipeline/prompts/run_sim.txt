You are working in the repository root (current working directory).
Do not prefix paths with "cocotb_ex/".

Run a single simulation for the AI DUT using make:
- Command: make -C sim sim TOPLEVEL={top_level} RTL_FILELISTS={rtl_filelist} COCOTB_TEST_MODULES={test_module} COCOTB_TESTCASE={testcase} WAVES=0 COV=0

Capture all stdout/stderr to {error_logs} (overwrite). Use a single bash command with pipefail and tee.

If the make command succeeds (exit 0):
- Remove {error_logs} if it exists.
- Print "SIM_PASS".

If it fails:
- Leave {error_logs} in place.
- Print "SIM_FAIL".

Constraints:
- Do NOT change directory (cd). Execute all commands from the current root.
- Use the exact path given for {error_logs}; do not add prefixes.
- Do not modify any other files.
