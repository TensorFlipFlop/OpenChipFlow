# Source Requirement Spec

The downstream handoff bundle shall contain a stable baseline summary, compatibility
constraints, incremental requirements, and a runnable cocotb verification plan.

The original `ai_dut` design hierarchy remains the reference baseline.
