# Source Requirement Overview

- Preserve the existing top-level wrapper and testcase naming unless the handoff manifest
  explicitly disambiguates a required change.
- Keep the verification flow compatible with the existing Verilator-based bundle.
- Provide enough semantic context that a downstream reviewer can compare the derived docs
  with the original request.
