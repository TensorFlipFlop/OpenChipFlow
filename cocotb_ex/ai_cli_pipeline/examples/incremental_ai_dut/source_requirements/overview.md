# Source Requirement Overview

- The handoff reviewer must be able to compare the original request against the derived
  baseline summary, compatibility constraints, incremental requirements, and testplan.
- The handoff manifest must keep all design assets under the canonical `rtl/`,
  `filelists/`, `tb/`, and `tests/` layout.
